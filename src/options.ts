interface Options {
    container: string,
    fnAnchor: string,
    refAnchor: string
}

const options: Options = {
    container: 'article',
    fnAnchor: '<<',
    refAnchor: '>>'
}

export default options;