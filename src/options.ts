interface Options {
  container: string;
  fnAnchor: string;
  refAnchor: string;
  background: string;
  text: string;
}

const options: Options = {
  container: "article",
  fnAnchor: ">>",
  refAnchor: "<<",
  background: "#fff",
  text: "#000",
};

export default options;
